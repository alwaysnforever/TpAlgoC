#indef WORDLE_H
#define WORDLE_H

#include<stdio.h>
#include<stdlib.h>
#include<string.h>
#include<time.h>

#define MAX_WORDS 6000
#define WORD_LENGTH 5
#define MAX_ATTEMPTS 6

#define COLOR_GREEN   "\x1b[42;30m"  // green background, black text
#define COLOR_YELLOW  "\x1b[43;30m"  // yellow background, black text
#define COLOR_BLACK   "\x1b[40;37m"  // black background, white text
#define COLOR_RESET   "\x1b[0m"      // reset to default

//-----GAME FUNCTION (PART 1) -----
int ReadDictionary(char dictionary[][WORD_LENGTH + 1]);
void ChooseRandomWord(char dictionary[][WORD_LENGTH + 1], int count, char *secret);
int IsValidWord(const char *word, char dictionary[][WORD_LENGTH + 1], int count);
void GetUserGuess(char *guess, char dictionary[][WORD_LENGTH + 1], int count);
void GiveFeedback(char guess[], char secret[], char feedback[]);
void PlayGame(char dictionary[][WORD_LENGTH + 1], int LoadedWords, char *secret);

//-------SOLVER FUNCTIONS (PART 2) ------
void ComputeFeedback(const char *guess, const char *secret, char *feedback);
int IsCompatible(const char *candidate, const char *guess, const char *feedback_target);
int FilterCandidates(char candidates[][WORD_LENGTH + 1], int count,
                     const char *guess, const char *feedback_target);
void InitializeCandidates(char candidates[][WORD_LENGTH + 1],
                          char dictionary[][WORD_LENGTH + 1], int count);
void ComputeLetterFrequencies(char candidates[][WORD_LENGTH + 1], int count, int freq[26]);
int ScoreWord(const char *word, int freq[26]);
void ChooseBestGuess(char candidatess[][WORD_LENGTH + 1], int count, char *BestGuess);
void Solver(char dictionary[][WORD_LENGTH + 1], int dictCount, const char *secret_word);
#endif
